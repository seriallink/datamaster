Register dynamic EventBridge Pipes to process table updates.

Usage:
  pipe
      Register pipes for all available tables.

  pipe --table <name>
      Register a pipe only for the specified table.

Flags:
  --table     Name of the table to register (optional)

Examples:
  pipe
  pipe --table customer

Note:
  Pipes connect Kinesis streams to Firehose dynamically, enabling real-time ingestion per table.
